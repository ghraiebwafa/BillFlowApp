# BillFlow Frontend

Frontend app placeholder for the BillFlow monorepo.

## Status

Frontend implementation has not been added yet.

## Expected Docker contract

The root `docker-compose.yml` expects:

- `Frontend/Dockerfile`
- container serving on port `3000`

## Next step

When you are ready, I can scaffold the frontend app (for example React + Vite) and wire it to:

- `VITE_AUTH_API_URL`
- `VITE_MANAGEMENT_API_URL`
